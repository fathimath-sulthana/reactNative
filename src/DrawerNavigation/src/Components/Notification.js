import { View, Text } from 'react-native'
import React from 'react'

const Notification = () => {
    return (
      <View style={{flex:1,justifyContent:'center',alignItems:'center'}}>
        <Text style={{fontSize:20,color:'red'}}>Notification</Text>
      </View>
    )
  }
export default Notification
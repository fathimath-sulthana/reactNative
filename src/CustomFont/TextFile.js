import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const TextFile = () => {
  return (
    <View style={{flex:1}}>
      <Text style={{fontSize:20,color:'green',fontFamily:'Poppins-ExtraBoldItalic'}}>TextFile</Text>
    </View>
  )
}

export default TextFile

const styles = StyleSheet.create({})